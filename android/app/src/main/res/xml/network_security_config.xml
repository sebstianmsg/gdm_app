<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite HTTP en texto plano SOLO contra el backend local (emulador -> 10.0.2.2,
  celular físico -> localhost/127.0.0.1) para desarrollo. Cualquier otro host
  (por ej. el deploy de Render en producción) sigue exigiendo HTTPS por default.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
