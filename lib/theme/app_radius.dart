/// Radios 1:1 con `public/css/styles.css`.
class AppRadius {
  AppRadius._();

  static const card = 18.0; // tarjetas: month-card, donut-card, movs-card
  static const modal = 12.0;
  static const input = 9.0;
  static const smallButton = 8.0;
  static const pill = 999.0; // chips de categoría, barras de leyenda
}
